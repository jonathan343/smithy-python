# Code generated by smithy-python-codegen DO NOT EDIT.

import asyncio
from base64 import b64decode
from copy import deepcopy
from datetime import datetime
import logging
from typing import Any, Self, cast

from smithy_aws_core.config import ConfigSource
from smithy_aws_core.identity import AWSCredentialsIdentity
from smithy_aws_core.identity.chain import IdentityChain
from smithy_core.aio.client import ClientCall, RequestPipeline
from smithy_core.aio.eventstream import (
    DuplexEventStream,
    InputEventStream,
    OutputEventStream,
)
from smithy_core.aio.interfaces import StreamingBlob
from smithy_core.aio.retries import RetryStrategyResolver
from smithy_core.aio.utils import close
from smithy_core.documents import Document
from smithy_core.exceptions import ExpectationNotMetError
from smithy_core.interceptors import InterceptorChain
from smithy_core.types import JsonString, TypedProperties
from smithy_http.aio.interfaces import HTTPClient
from smithy_http.plugins import user_agent_plugin

from .config import AsyncRestJsonProtocolConfig, Plugin
from .models import (
    ALL_QUERY_STRING_TYPES,
    AllQueryStringTypesInput,
    AllQueryStringTypesOutput,
    CONSTANT_AND_VARIABLE_QUERY_STRING,
    CONSTANT_QUERY_STRING,
    CONTENT_TYPE_PARAMETERS,
    ClientOptionalDefaults,
    ConstantAndVariableQueryStringInput,
    ConstantAndVariableQueryStringOutput,
    ConstantQueryStringInput,
    ConstantQueryStringOutput,
    ContentTypeParametersInput,
    ContentTypeParametersOutput,
    DATETIME_OFFSETS,
    DOCUMENT_TYPE,
    DOCUMENT_TYPE_AS_MAP_VALUE,
    DOCUMENT_TYPE_AS_PAYLOAD,
    DUPLEX_STREAM,
    DUPLEX_STREAM_WITH_DISTINCT_STREAMS,
    DUPLEX_STREAM_WITH_INITIAL_MESSAGES,
    DatetimeOffsetsInput,
    DatetimeOffsetsOutput,
    Defaults,
    DocumentTypeAsMapValueInput,
    DocumentTypeAsMapValueOutput,
    DocumentTypeAsPayloadInput,
    DocumentTypeAsPayloadOutput,
    DocumentTypeInput,
    DocumentTypeOutput,
    DuplexStreamInput,
    DuplexStreamOutput,
    DuplexStreamWithDistinctStreamsInput,
    DuplexStreamWithDistinctStreamsOutput,
    DuplexStreamWithInitialMessagesInput,
    DuplexStreamWithInitialMessagesOutput,
    EMPTY_INPUT_AND_EMPTY_OUTPUT,
    ENDPOINT_OPERATION,
    ENDPOINT_WITH_HOST_LABEL_OPERATION,
    EmptyInputAndEmptyOutputInput,
    EmptyInputAndEmptyOutputOutput,
    EndpointOperationInput,
    EndpointOperationOutput,
    EndpointWithHostLabelOperationInput,
    EndpointWithHostLabelOperationOutput,
    EventStream,
    FRACTIONAL_SECONDS,
    FractionalSecondsInput,
    FractionalSecondsOutput,
    GREETING_WITH_ERRORS,
    GreetingStruct,
    GreetingWithErrorsInput,
    GreetingWithErrorsOutput,
    HOST_WITH_PATH_OPERATION,
    HTTP_CHECKSUM_REQUIRED,
    HTTP_EMPTY_PREFIX_HEADERS,
    HTTP_ENUM_PAYLOAD,
    HTTP_PAYLOAD_TRAITS,
    HTTP_PAYLOAD_TRAITS_WITH_MEDIA_TYPE,
    HTTP_PAYLOAD_WITH_STRUCTURE,
    HTTP_PAYLOAD_WITH_UNION,
    HTTP_PREFIX_HEADERS,
    HTTP_PREFIX_HEADERS_IN_RESPONSE,
    HTTP_QUERY_PARAMS_ONLY_OPERATION,
    HTTP_REQUEST_WITH_FLOAT_LABELS,
    HTTP_REQUEST_WITH_GREEDY_LABEL_IN_PATH,
    HTTP_REQUEST_WITH_LABELS,
    HTTP_REQUEST_WITH_LABELS_AND_TIMESTAMP_FORMAT,
    HTTP_REQUEST_WITH_REGEX_LITERAL,
    HTTP_RESPONSE_CODE,
    HTTP_STRING_PAYLOAD,
    HostWithPathOperationInput,
    HostWithPathOperationOutput,
    HttpChecksumRequiredInput,
    HttpChecksumRequiredOutput,
    HttpEmptyPrefixHeadersInput,
    HttpEmptyPrefixHeadersOutput,
    HttpEnumPayloadInput,
    HttpEnumPayloadOutput,
    HttpPayloadTraitsInput,
    HttpPayloadTraitsOutput,
    HttpPayloadTraitsWithMediaTypeInput,
    HttpPayloadTraitsWithMediaTypeOutput,
    HttpPayloadWithStructureInput,
    HttpPayloadWithStructureOutput,
    HttpPayloadWithUnionInput,
    HttpPayloadWithUnionOutput,
    HttpPrefixHeadersInResponseInput,
    HttpPrefixHeadersInResponseOutput,
    HttpPrefixHeadersInput,
    HttpPrefixHeadersOutput,
    HttpQueryParamsOnlyOperationInput,
    HttpQueryParamsOnlyOperationOutput,
    HttpRequestWithFloatLabelsInput,
    HttpRequestWithFloatLabelsOutput,
    HttpRequestWithGreedyLabelInPathInput,
    HttpRequestWithGreedyLabelInPathOutput,
    HttpRequestWithLabelsAndTimestampFormatInput,
    HttpRequestWithLabelsAndTimestampFormatOutput,
    HttpRequestWithLabelsInput,
    HttpRequestWithLabelsOutput,
    HttpRequestWithRegexLiteralInput,
    HttpRequestWithRegexLiteralOutput,
    HttpResponseCodeInput,
    HttpResponseCodeOutput,
    HttpStringPayloadInput,
    HttpStringPayloadOutput,
    IGNORE_QUERY_PARAMS_IN_RESPONSE,
    INPUT_AND_OUTPUT_WITH_HEADERS,
    INPUT_STREAM,
    INPUT_STREAM_WITH_INITIAL_REQUEST,
    IgnoreQueryParamsInResponseInput,
    IgnoreQueryParamsInResponseOutput,
    InputAndOutputWithHeadersInput,
    InputAndOutputWithHeadersOutput,
    InputStreamInput,
    InputStreamOutput,
    InputStreamWithInitialRequestInput,
    InputStreamWithInitialRequestOutput,
    JSON_BLOBS,
    JSON_ENUMS,
    JSON_INT_ENUMS,
    JSON_LISTS,
    JSON_MAPS,
    JSON_TIMESTAMPS,
    JSON_UNIONS,
    JsonBlobsInput,
    JsonBlobsOutput,
    JsonEnumsInput,
    JsonEnumsOutput,
    JsonIntEnumsInput,
    JsonIntEnumsOutput,
    JsonListsInput,
    JsonListsOutput,
    JsonMapsInput,
    JsonMapsOutput,
    JsonTimestampsInput,
    JsonTimestampsOutput,
    JsonUnionsInput,
    JsonUnionsOutput,
    MALFORMED_ACCEPT_WITH_BODY,
    MALFORMED_ACCEPT_WITH_GENERIC_STRING,
    MALFORMED_ACCEPT_WITH_PAYLOAD,
    MALFORMED_BLOB,
    MALFORMED_BOOLEAN,
    MALFORMED_BYTE,
    MALFORMED_CONTENT_TYPE_WITHOUT_BODY,
    MALFORMED_CONTENT_TYPE_WITHOUT_BODY_EMPTY_INPUT,
    MALFORMED_CONTENT_TYPE_WITH_BODY,
    MALFORMED_CONTENT_TYPE_WITH_GENERIC_STRING,
    MALFORMED_CONTENT_TYPE_WITH_PAYLOAD,
    MALFORMED_DOUBLE,
    MALFORMED_FLOAT,
    MALFORMED_INTEGER,
    MALFORMED_LIST,
    MALFORMED_LONG,
    MALFORMED_MAP,
    MALFORMED_REQUEST_BODY,
    MALFORMED_SHORT,
    MALFORMED_STRING,
    MALFORMED_TIMESTAMP_BODY_DATE_TIME,
    MALFORMED_TIMESTAMP_BODY_DEFAULT,
    MALFORMED_TIMESTAMP_BODY_HTTP_DATE,
    MALFORMED_TIMESTAMP_HEADER_DATE_TIME,
    MALFORMED_TIMESTAMP_HEADER_DEFAULT,
    MALFORMED_TIMESTAMP_HEADER_EPOCH,
    MALFORMED_TIMESTAMP_PATH_DEFAULT,
    MALFORMED_TIMESTAMP_PATH_EPOCH,
    MALFORMED_TIMESTAMP_PATH_HTTP_DATE,
    MALFORMED_TIMESTAMP_QUERY_DEFAULT,
    MALFORMED_TIMESTAMP_QUERY_EPOCH,
    MALFORMED_TIMESTAMP_QUERY_HTTP_DATE,
    MALFORMED_UNION,
    MEDIA_TYPE_HEADER,
    MalformedAcceptWithBodyInput,
    MalformedAcceptWithBodyOutput,
    MalformedAcceptWithGenericStringInput,
    MalformedAcceptWithGenericStringOutput,
    MalformedAcceptWithPayloadInput,
    MalformedAcceptWithPayloadOutput,
    MalformedBlobInput,
    MalformedBlobOutput,
    MalformedBooleanInput,
    MalformedBooleanOutput,
    MalformedByteInput,
    MalformedByteOutput,
    MalformedContentTypeWithBodyInput,
    MalformedContentTypeWithBodyOutput,
    MalformedContentTypeWithGenericStringInput,
    MalformedContentTypeWithGenericStringOutput,
    MalformedContentTypeWithPayloadInput,
    MalformedContentTypeWithPayloadOutput,
    MalformedContentTypeWithoutBodyEmptyInputInput,
    MalformedContentTypeWithoutBodyEmptyInputOutput,
    MalformedContentTypeWithoutBodyInput,
    MalformedContentTypeWithoutBodyOutput,
    MalformedDoubleInput,
    MalformedDoubleOutput,
    MalformedFloatInput,
    MalformedFloatOutput,
    MalformedIntegerInput,
    MalformedIntegerOutput,
    MalformedListInput,
    MalformedListOutput,
    MalformedLongInput,
    MalformedLongOutput,
    MalformedMapInput,
    MalformedMapOutput,
    MalformedRequestBodyInput,
    MalformedRequestBodyOutput,
    MalformedShortInput,
    MalformedShortOutput,
    MalformedStringInput,
    MalformedStringOutput,
    MalformedTimestampBodyDateTimeInput,
    MalformedTimestampBodyDateTimeOutput,
    MalformedTimestampBodyDefaultInput,
    MalformedTimestampBodyDefaultOutput,
    MalformedTimestampBodyHttpDateInput,
    MalformedTimestampBodyHttpDateOutput,
    MalformedTimestampHeaderDateTimeInput,
    MalformedTimestampHeaderDateTimeOutput,
    MalformedTimestampHeaderDefaultInput,
    MalformedTimestampHeaderDefaultOutput,
    MalformedTimestampHeaderEpochInput,
    MalformedTimestampHeaderEpochOutput,
    MalformedTimestampPathDefaultInput,
    MalformedTimestampPathDefaultOutput,
    MalformedTimestampPathEpochInput,
    MalformedTimestampPathEpochOutput,
    MalformedTimestampPathHttpDateInput,
    MalformedTimestampPathHttpDateOutput,
    MalformedTimestampQueryDefaultInput,
    MalformedTimestampQueryDefaultOutput,
    MalformedTimestampQueryEpochInput,
    MalformedTimestampQueryEpochOutput,
    MalformedTimestampQueryHttpDateInput,
    MalformedTimestampQueryHttpDateOutput,
    MalformedUnionInput,
    MalformedUnionOutput,
    MediaTypeHeaderInput,
    MediaTypeHeaderOutput,
    MyUnion,
    NO_INPUT_AND_NO_OUTPUT,
    NO_INPUT_AND_OUTPUT,
    NULL_AND_EMPTY_HEADERS_CLIENT,
    NULL_AND_EMPTY_HEADERS_SERVER,
    NestedPayload,
    NoInputAndNoOutputInput,
    NoInputAndNoOutputOutput,
    NoInputAndOutputInput,
    NoInputAndOutputOutput,
    NullAndEmptyHeadersClientInput,
    NullAndEmptyHeadersClientOutput,
    NullAndEmptyHeadersServerInput,
    NullAndEmptyHeadersServerOutput,
    OMITS_NULL_SERIALIZES_EMPTY_STRING,
    OMITS_SERIALIZING_EMPTY_LISTS,
    OPERATION_WITH_DEFAULTS,
    OPERATION_WITH_NESTED_STRUCTURE,
    OUTPUT_STREAM,
    OUTPUT_STREAM_WITH_INITIAL_RESPONSE,
    OmitsNullSerializesEmptyStringInput,
    OmitsNullSerializesEmptyStringOutput,
    OmitsSerializingEmptyListsInput,
    OmitsSerializingEmptyListsOutput,
    OperationWithDefaultsInput,
    OperationWithDefaultsOutput,
    OperationWithNestedStructureInput,
    OperationWithNestedStructureOutput,
    OutputStreamInput,
    OutputStreamOutput,
    OutputStreamWithInitialResponseInput,
    OutputStreamWithInitialResponseOutput,
    POST_PLAYER_ACTION,
    POST_UNION_WITH_JSON_NAME,
    PUT_WITH_CONTENT_ENCODING,
    PayloadConfig,
    PlayerAction,
    PostPlayerActionInput,
    PostPlayerActionOutput,
    PostUnionWithJsonNameInput,
    PostUnionWithJsonNameOutput,
    PutWithContentEncodingInput,
    PutWithContentEncodingOutput,
    QUERY_IDEMPOTENCY_TOKEN_AUTO_FILL,
    QUERY_PARAMS_AS_STRING_LIST_MAP,
    QUERY_PRECEDENCE,
    QueryIdempotencyTokenAutoFillInput,
    QueryIdempotencyTokenAutoFillOutput,
    QueryParamsAsStringListMapInput,
    QueryParamsAsStringListMapOutput,
    QueryPrecedenceInput,
    QueryPrecedenceOutput,
    RECURSIVE_SHAPES,
    RESPONSE_CODE_HTTP_FALLBACK,
    RESPONSE_CODE_REQUIRED,
    RecursiveShapesInput,
    RecursiveShapesInputOutputNested1,
    RecursiveShapesOutput,
    ResponseCodeHttpFallbackInput,
    ResponseCodeHttpFallbackOutput,
    ResponseCodeRequiredInput,
    ResponseCodeRequiredOutput,
    SIMPLE_SCALAR_PROPERTIES,
    SPARSE_JSON_LISTS,
    SPARSE_JSON_MAPS,
    STREAMING_TRAITS,
    STREAMING_TRAITS_REQUIRE_LENGTH,
    STREAMING_TRAITS_WITH_MEDIA_TYPE,
    SimpleScalarPropertiesInput,
    SimpleScalarPropertiesOutput,
    SimpleUnion,
    SparseJsonListsInput,
    SparseJsonListsOutput,
    SparseJsonMapsInput,
    SparseJsonMapsOutput,
    StreamingTraitsInput,
    StreamingTraitsOutput,
    StreamingTraitsRequireLengthInput,
    StreamingTraitsRequireLengthOutput,
    StreamingTraitsWithMediaTypeInput,
    StreamingTraitsWithMediaTypeOutput,
    StructureListMember,
    TEST_BODY_STRUCTURE,
    TEST_GET_NO_INPUT_NO_PAYLOAD,
    TEST_GET_NO_PAYLOAD,
    TEST_PAYLOAD_BLOB,
    TEST_PAYLOAD_STRUCTURE,
    TEST_POST_NO_INPUT_NO_PAYLOAD,
    TEST_POST_NO_PAYLOAD,
    TIMESTAMP_FORMAT_HEADERS,
    TestBodyStructureInput,
    TestBodyStructureOutput,
    TestConfig,
    TestGetNoInputNoPayloadInput,
    TestGetNoInputNoPayloadOutput,
    TestGetNoPayloadInput,
    TestGetNoPayloadOutput,
    TestPayloadBlobInput,
    TestPayloadBlobOutput,
    TestPayloadStructureInput,
    TestPayloadStructureOutput,
    TestPostNoInputNoPayloadInput,
    TestPostNoInputNoPayloadOutput,
    TestPostNoPayloadInput,
    TestPostNoPayloadOutput,
    TimestampFormatHeadersInput,
    TimestampFormatHeadersOutput,
    TopLevel,
    UNIT_INPUT_AND_OUTPUT,
    UnionPayload,
    UnionWithJsonName,
    UnitInputAndOutputInput,
    UnitInputAndOutputOutput,
    _EventStreamDeserializer,
)
from .user_agent import aws_user_agent_plugin


logger = logging.getLogger(__name__)


class AsyncRestJsonProtocolClient:
    """Client for RestJson"""

    def __init__(
        self,
        config: AsyncRestJsonProtocolConfig | None = None,
        plugins: list[Plugin] | None = None,
    ):
        """
        Constructor for `AsyncRestJsonProtocolClient`.

        Args:
            config:
                Optional configuration for the client. Here you can set things like
                the endpoint for HTTP services or auth credentials.
            plugins:
                A list of callables applied once to the client's base configuration.
                Their changes are inherited by every operation invocation.
        """
        self._config = config
        self._plugins = plugins
        self._derive_lock = asyncio.Lock()
        self._setup_done = False
        self._closed = False
        self._retry_strategy_resolver = RetryStrategyResolver()
        self._client_plugins: list[Plugin] = [aws_user_agent_plugin, user_agent_plugin]

    async def _ensure_setup(self) -> None:
        if not self._setup_done:
            async with self._derive_lock:
                if not self._setup_done:
                    if self._config is None:
                        config = await AsyncRestJsonProtocolConfig.resolve()
                    else:
                        # Copy so plugins don't mutate the caller's config.
                        config = deepcopy(self._config)
                    for plugin in self._client_plugins:
                        plugin(config)
                    if self._plugins:
                        for plugin in self._plugins:
                            plugin(config)
                    self._config = config

                    if self._config.aws_credentials_identity_resolver is None:
                        config_context = self._config.resolution_context()
                        config_file = None
                        profile_name = None
                        if config_context is not None:
                            config_file = await config_context.parsed_profiles()
                            if config_context.profile_source is ConfigSource.OVERRIDE:
                                profile_name = config_context.profile_name
                        self._config.aws_credentials_identity_resolver = (
                            await IdentityChain.create(
                                AWSCredentialsIdentity,
                                config_file=config_file,
                                profile_name=profile_name,
                                region_override=self._config.region,
                                http_client=cast(
                                    HTTPClient | None, self._config.transport
                                ),
                            )
                        )
                    self._setup_done = True

    async def close(self) -> None:
        """Close this client and any resources held by its transport."""
        if self._closed:
            return
        async with self._derive_lock:
            if self._closed:
                return
            self._closed = True
            if self._setup_done and self._config is not None:
                await close(self._config.transport)

    async def __aenter__(self) -> Self:
        if self._closed:
            raise RuntimeError("Cannot enter a client that has been closed.")
        return self

    async def __aexit__(self, exc_type: Any, exc_value: Any, traceback: Any) -> None:
        await self.close()

    async def all_query_string_types(
        self,
        *,
        query_string: str | None = None,
        query_string_list: list[str] | None = None,
        query_string_set: list[str] | None = None,
        query_byte: int | None = None,
        query_short: int | None = None,
        query_integer: int | None = None,
        query_integer_list: list[int] | None = None,
        query_integer_set: list[int] | None = None,
        query_long: int | None = None,
        query_float: float | None = None,
        query_double: float | None = None,
        query_double_list: list[float] | None = None,
        query_boolean: bool | None = None,
        query_boolean_list: list[bool] | None = None,
        query_timestamp: datetime | None = None,
        query_timestamp_list: list[datetime] | None = None,
        query_enum: str | None = None,
        query_enum_list: list[str] | None = None,
        query_integer_enum: int | None = None,
        query_integer_enum_list: list[int] | None = None,
        query_params_map_of_string_list: dict[str, list[str]] | None = None,
        plugins: list[Plugin] | None = None,
    ) -> AllQueryStringTypesOutput:
        """
        This example uses all query string types.

        Args:
            query_string:
                The `query_string` input member.
            query_string_list:
                The `query_string_list` input member.
            query_string_set:
                The `query_string_set` input member.
            query_byte:
                The `query_byte` input member.
            query_short:
                The `query_short` input member.
            query_integer:
                The `query_integer` input member.
            query_integer_list:
                The `query_integer_list` input member.
            query_integer_set:
                The `query_integer_set` input member.
            query_long:
                The `query_long` input member.
            query_float:
                The `query_float` input member.
            query_double:
                The `query_double` input member.
            query_double_list:
                The `query_double_list` input member.
            query_boolean:
                The `query_boolean` input member.
            query_boolean_list:
                The `query_boolean_list` input member.
            query_timestamp:
                The `query_timestamp` input member.
            query_timestamp_list:
                The `query_timestamp_list` input member.
            query_enum:
                The `query_enum` input member.
            query_enum_list:
                The `query_enum_list` input member.
            query_integer_enum:
                The `query_integer_enum` input member.
            query_integer_enum_list:
                The `query_integer_enum_list` input member.
            query_params_map_of_string_list:
                The `query_params_map_of_string_list` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `AllQueryStringTypesOutput`.
        """
        input = AllQueryStringTypesInput(
            query_string=query_string,
            query_string_list=query_string_list,
            query_string_set=query_string_set,
            query_byte=query_byte,
            query_short=query_short,
            query_integer=query_integer,
            query_integer_list=query_integer_list,
            query_integer_set=query_integer_set,
            query_long=query_long,
            query_float=query_float,
            query_double=query_double,
            query_double_list=query_double_list,
            query_boolean=query_boolean,
            query_boolean_list=query_boolean_list,
            query_timestamp=query_timestamp,
            query_timestamp_list=query_timestamp_list,
            query_enum=query_enum,
            query_enum_list=query_enum_list,
            query_integer_enum=query_integer_enum,
            query_integer_enum_list=query_integer_enum_list,
            query_params_map_of_string_list=query_params_map_of_string_list,
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=ALL_QUERY_STRING_TYPES,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def constant_and_variable_query_string(
        self,
        *,
        baz: str | None = None,
        maybe_set: str | None = None,
        plugins: list[Plugin] | None = None,
    ) -> ConstantAndVariableQueryStringOutput:
        """
        This example uses fixed query string params and variable query string
        params. The fixed query string parameters and variable parameters must
        both be serialized (implementations may need to merge them together).

        Args:
            baz:
                The `baz` input member.
            maybe_set:
                The `maybe_set` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `ConstantAndVariableQueryStringOutput`.
        """
        input = ConstantAndVariableQueryStringInput(baz=baz, maybe_set=maybe_set)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=CONSTANT_AND_VARIABLE_QUERY_STRING,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def constant_query_string(
        self, *, hello: str | None = None, plugins: list[Plugin] | None = None
    ) -> ConstantQueryStringOutput:
        """
        This example uses a constant query string parameters and a label. This
        simply tests that labels and query string parameters are compatible. The
        fixed query string parameter named \"hello\" should in no way conflict
        with the label, `{hello}`.

        Args:
            hello:
                The `hello` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `ConstantQueryStringOutput`.
        """
        input = ConstantQueryStringInput(hello=hello)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=CONSTANT_QUERY_STRING,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def content_type_parameters(
        self, *, value: int | None = None, plugins: list[Plugin] | None = None
    ) -> ContentTypeParametersOutput:
        """
        The example tests how servers must support requests containing a
        `Content-Type` header with parameters.

        Args:
            value:
                The `value` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `ContentTypeParametersOutput`.
        """
        input = ContentTypeParametersInput(value=value)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=CONTENT_TYPE_PARAMETERS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def datetime_offsets(
        self, *, plugins: list[Plugin] | None = None
    ) -> DatetimeOffsetsOutput:
        """
        Invokes the DatetimeOffsets operation.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `DatetimeOffsetsOutput`.
        """
        input = DatetimeOffsetsInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=DATETIME_OFFSETS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def document_type(
        self,
        *,
        string_value: str | None = None,
        document_value: Document | None = None,
        plugins: list[Plugin] | None = None,
    ) -> DocumentTypeOutput:
        """
        This example serializes a document as part of the payload.

        Args:
            string_value:
                The `string_value` input member.
            document_value:
                The `document_value` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `DocumentTypeOutput`.
        """
        input = DocumentTypeInput(
            string_value=string_value, document_value=document_value
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=DOCUMENT_TYPE,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def document_type_as_map_value(
        self,
        *,
        doc_valued_map: dict[str, Document] | None = None,
        plugins: list[Plugin] | None = None,
    ) -> DocumentTypeAsMapValueOutput:
        """
        This example serializes documents as the value of maps.

        Args:
            doc_valued_map:
                The `doc_valued_map` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `DocumentTypeAsMapValueOutput`.
        """
        input = DocumentTypeAsMapValueInput(doc_valued_map=doc_valued_map)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=DOCUMENT_TYPE_AS_MAP_VALUE,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def document_type_as_payload(
        self,
        *,
        document_value: Document | None = None,
        plugins: list[Plugin] | None = None,
    ) -> DocumentTypeAsPayloadOutput:
        """
        This example serializes a document as the entire HTTP payload.

        Args:
            document_value:
                The `document_value` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `DocumentTypeAsPayloadOutput`.
        """
        input = DocumentTypeAsPayloadInput(document_value=document_value)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=DOCUMENT_TYPE_AS_PAYLOAD,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def duplex_stream(
        self, *, plugins: list[Plugin] | None = None
    ) -> DuplexEventStream[EventStream, EventStream, DuplexStreamOutput]:
        """
        Invokes the DuplexStream operation.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            A `DuplexEventStream` for bidirectional streaming.
        """
        input = DuplexStreamInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=DUPLEX_STREAM,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline.duplex_stream(
            call, EventStream, EventStream, _EventStreamDeserializer().deserialize
        )

    async def duplex_stream_with_distinct_streams(
        self, *, plugins: list[Plugin] | None = None
    ) -> InputEventStream[EventStream, DuplexStreamWithDistinctStreamsOutput]:
        """
        Invokes the DuplexStreamWithDistinctStreams operation.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An `InputEventStream` for client-to-server streaming.
        """
        input = DuplexStreamWithDistinctStreamsInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=DUPLEX_STREAM_WITH_DISTINCT_STREAMS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline.input_stream(call, EventStream)

    async def duplex_stream_with_initial_messages(
        self,
        *,
        initial_request_member: str | None = None,
        plugins: list[Plugin] | None = None,
    ) -> DuplexEventStream[
        EventStream, EventStream, DuplexStreamWithInitialMessagesOutput
    ]:
        """
        Invokes the DuplexStreamWithInitialMessages operation.

        Args:
            initial_request_member:
                The `initial_request_member` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            A `DuplexEventStream` for bidirectional streaming.
        """
        input = DuplexStreamWithInitialMessagesInput(
            initial_request_member=initial_request_member
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=DUPLEX_STREAM_WITH_INITIAL_MESSAGES,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline.duplex_stream(
            call, EventStream, EventStream, _EventStreamDeserializer().deserialize
        )

    async def empty_input_and_empty_output(
        self, *, plugins: list[Plugin] | None = None
    ) -> EmptyInputAndEmptyOutputOutput:
        """
        The example tests how requests and responses are serialized when
        there's no request or response payload because the operation has an
        empty input and empty output structure that reuses the same shape. While
        this should be rare, code generators must support this.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `EmptyInputAndEmptyOutputOutput`.
        """
        input = EmptyInputAndEmptyOutputInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=EMPTY_INPUT_AND_EMPTY_OUTPUT,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def endpoint_operation(
        self, *, plugins: list[Plugin] | None = None
    ) -> EndpointOperationOutput:
        """
        Invokes the EndpointOperation operation.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `EndpointOperationOutput`.
        """
        input = EndpointOperationInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=ENDPOINT_OPERATION,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def endpoint_with_host_label_operation(
        self, *, label: str | None = None, plugins: list[Plugin] | None = None
    ) -> EndpointWithHostLabelOperationOutput:
        """
        Invokes the EndpointWithHostLabelOperation operation.

        Args:
            label:
                The `label` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `EndpointWithHostLabelOperationOutput`.
        """
        input = EndpointWithHostLabelOperationInput(label=label)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=ENDPOINT_WITH_HOST_LABEL_OPERATION,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def fractional_seconds(
        self, *, plugins: list[Plugin] | None = None
    ) -> FractionalSecondsOutput:
        """
        Invokes the FractionalSeconds operation.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `FractionalSecondsOutput`.
        """
        input = FractionalSecondsInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=FRACTIONAL_SECONDS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def greeting_with_errors(
        self, *, plugins: list[Plugin] | None = None
    ) -> GreetingWithErrorsOutput:
        """
        This operation has four possible return values: 1. A successful response
        in the form of GreetingWithErrorsOutput 2. An InvalidGreeting error. 3.
        A BadRequest error. 4. A FooError. Implementations must be able to
        successfully take a response and properly (de)serialize successful and
        error responses based on the the presence of the

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `GreetingWithErrorsOutput`.
        """
        input = GreetingWithErrorsInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=GREETING_WITH_ERRORS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def host_with_path_operation(
        self, *, plugins: list[Plugin] | None = None
    ) -> HostWithPathOperationOutput:
        """
        Invokes the HostWithPathOperation operation.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `HostWithPathOperationOutput`.
        """
        input = HostWithPathOperationInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=HOST_WITH_PATH_OPERATION,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def http_checksum_required(
        self, *, foo: str | None = None, plugins: list[Plugin] | None = None
    ) -> HttpChecksumRequiredOutput:
        """
        This example tests httpChecksumRequired trait

        Args:
            foo:
                The `foo` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `HttpChecksumRequiredOutput`.
        """
        input = HttpChecksumRequiredInput(foo=foo)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=HTTP_CHECKSUM_REQUIRED,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def http_empty_prefix_headers(
        self,
        *,
        prefix_headers: dict[str, str] | None = None,
        specific_header: str | None = None,
        plugins: list[Plugin] | None = None,
    ) -> HttpEmptyPrefixHeadersOutput:
        """
        Clients that perform this test extract all headers from the response.

        Args:
            prefix_headers:
                The `prefix_headers` input member.
            specific_header:
                The `specific_header` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `HttpEmptyPrefixHeadersOutput`.
        """
        input = HttpEmptyPrefixHeadersInput(
            prefix_headers=prefix_headers, specific_header=specific_header
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=HTTP_EMPTY_PREFIX_HEADERS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def http_enum_payload(
        self, *, payload: str | None = None, plugins: list[Plugin] | None = None
    ) -> HttpEnumPayloadOutput:
        """
        Invokes the HttpEnumPayload operation.

        Args:
            payload:
                The `payload` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `HttpEnumPayloadOutput`.
        """
        input = HttpEnumPayloadInput(payload=payload)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=HTTP_ENUM_PAYLOAD,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def http_payload_traits(
        self,
        *,
        foo: str | None = None,
        blob: bytes | None = None,
        plugins: list[Plugin] | None = None,
    ) -> HttpPayloadTraitsOutput:
        """
        This example serializes a blob shape in the payload. In this example, no
        JSON document is synthesized because the payload is not a structure or a
        union type.

        Args:
            foo:
                The `foo` input member.
            blob:
                The `blob` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `HttpPayloadTraitsOutput`.
        """
        input = HttpPayloadTraitsInput(foo=foo, blob=blob)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=HTTP_PAYLOAD_TRAITS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def http_payload_traits_with_media_type(
        self,
        *,
        foo: str | None = None,
        blob: bytes | None = None,
        plugins: list[Plugin] | None = None,
    ) -> HttpPayloadTraitsWithMediaTypeOutput:
        """
        This example uses a `@mediaType` trait on the payload to force a
        custom content-type to be serialized.

        Args:
            foo:
                The `foo` input member.
            blob:
                The `blob` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `HttpPayloadTraitsWithMediaTypeOutput`.
        """
        input = HttpPayloadTraitsWithMediaTypeInput(foo=foo, blob=blob)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=HTTP_PAYLOAD_TRAITS_WITH_MEDIA_TYPE,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def http_payload_with_structure(
        self,
        *,
        nested: NestedPayload | None = None,
        plugins: list[Plugin] | None = None,
    ) -> HttpPayloadWithStructureOutput:
        """
        This example serializes a structure in the payload. Note that
        serializing a structure changes the wrapper element name to match the
        targeted structure.

        Args:
            nested:
                The `nested` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `HttpPayloadWithStructureOutput`.
        """
        input = HttpPayloadWithStructureInput(nested=nested)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=HTTP_PAYLOAD_WITH_STRUCTURE,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def http_payload_with_union(
        self, *, nested: UnionPayload | None = None, plugins: list[Plugin] | None = None
    ) -> HttpPayloadWithUnionOutput:
        """
        This example serializes a union in the payload.

        Args:
            nested:
                The `nested` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `HttpPayloadWithUnionOutput`.
        """
        input = HttpPayloadWithUnionInput(nested=nested)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=HTTP_PAYLOAD_WITH_UNION,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def http_prefix_headers(
        self,
        *,
        foo: str | None = None,
        foo_map: dict[str, str] | None = None,
        plugins: list[Plugin] | None = None,
    ) -> HttpPrefixHeadersOutput:
        """
        This examples adds headers to the input of a request and response by
        prefix.

        Args:
            foo:
                The `foo` input member.
            foo_map:
                The `foo_map` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `HttpPrefixHeadersOutput`.
        """
        input = HttpPrefixHeadersInput(foo=foo, foo_map=foo_map)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=HTTP_PREFIX_HEADERS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def http_prefix_headers_in_response(
        self, *, plugins: list[Plugin] | None = None
    ) -> HttpPrefixHeadersInResponseOutput:
        """
        Clients that perform this test extract all headers from the response.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `HttpPrefixHeadersInResponseOutput`.
        """
        input = HttpPrefixHeadersInResponseInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=HTTP_PREFIX_HEADERS_IN_RESPONSE,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def http_query_params_only_operation(
        self,
        *,
        query_map: dict[str, str] | None = None,
        plugins: list[Plugin] | None = None,
    ) -> HttpQueryParamsOnlyOperationOutput:
        """
        This example tests httpQueryParams when no other query parameters exist.

        Args:
            query_map:
                The `query_map` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `HttpQueryParamsOnlyOperationOutput`.
        """
        input = HttpQueryParamsOnlyOperationInput(query_map=query_map)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=HTTP_QUERY_PARAMS_ONLY_OPERATION,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def http_request_with_float_labels(
        self,
        *,
        float_: float | None = None,
        double: float | None = None,
        plugins: list[Plugin] | None = None,
    ) -> HttpRequestWithFloatLabelsOutput:
        """
        Invokes the HttpRequestWithFloatLabels operation.

        Args:
            float_:
                The `float_` input member.
            double:
                The `double` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `HttpRequestWithFloatLabelsOutput`.
        """
        input = HttpRequestWithFloatLabelsInput(float_=float_, double=double)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=HTTP_REQUEST_WITH_FLOAT_LABELS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def http_request_with_greedy_label_in_path(
        self,
        *,
        foo: str | None = None,
        baz: str | None = None,
        plugins: list[Plugin] | None = None,
    ) -> HttpRequestWithGreedyLabelInPathOutput:
        """
        Invokes the HttpRequestWithGreedyLabelInPath operation.

        Args:
            foo:
                The `foo` input member.
            baz:
                The `baz` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `HttpRequestWithGreedyLabelInPathOutput`.
        """
        input = HttpRequestWithGreedyLabelInPathInput(foo=foo, baz=baz)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=HTTP_REQUEST_WITH_GREEDY_LABEL_IN_PATH,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def http_request_with_labels(
        self,
        *,
        string: str | None = None,
        short: int | None = None,
        integer: int | None = None,
        long: int | None = None,
        float_: float | None = None,
        double: float | None = None,
        boolean: bool | None = None,
        timestamp: datetime | None = None,
        plugins: list[Plugin] | None = None,
    ) -> HttpRequestWithLabelsOutput:
        """
        The example tests how requests are serialized when there's no input
        payload but there are HTTP labels.

        Args:
            string:
                The `string` input member.
            short:
                The `short` input member.
            integer:
                The `integer` input member.
            long:
                The `long` input member.
            float_:
                The `float_` input member.
            double:
                The `double` input member.
            boolean:
                Serialized in the path as true or false.
            timestamp:
                Note that this member has no format, so it's serialized as an RFC 3399
                date-time.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `HttpRequestWithLabelsOutput`.
        """
        input = HttpRequestWithLabelsInput(
            string=string,
            short=short,
            integer=integer,
            long=long,
            float_=float_,
            double=double,
            boolean=boolean,
            timestamp=timestamp,
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=HTTP_REQUEST_WITH_LABELS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def http_request_with_labels_and_timestamp_format(
        self,
        *,
        member_epoch_seconds: datetime | None = None,
        member_http_date: datetime | None = None,
        member_date_time: datetime | None = None,
        default_format: datetime | None = None,
        target_epoch_seconds: datetime | None = None,
        target_http_date: datetime | None = None,
        target_date_time: datetime | None = None,
        plugins: list[Plugin] | None = None,
    ) -> HttpRequestWithLabelsAndTimestampFormatOutput:
        """
        The example tests how requests serialize different timestamp formats in
        the URI path.

        Args:
            member_epoch_seconds:
                The `member_epoch_seconds` input member.
            member_http_date:
                The `member_http_date` input member.
            member_date_time:
                The `member_date_time` input member.
            default_format:
                The `default_format` input member.
            target_epoch_seconds:
                The `target_epoch_seconds` input member.
            target_http_date:
                The `target_http_date` input member.
            target_date_time:
                The `target_date_time` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `HttpRequestWithLabelsAndTimestampFormatOutput`.
        """
        input = HttpRequestWithLabelsAndTimestampFormatInput(
            member_epoch_seconds=member_epoch_seconds,
            member_http_date=member_http_date,
            member_date_time=member_date_time,
            default_format=default_format,
            target_epoch_seconds=target_epoch_seconds,
            target_http_date=target_http_date,
            target_date_time=target_date_time,
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=HTTP_REQUEST_WITH_LABELS_AND_TIMESTAMP_FORMAT,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def http_request_with_regex_literal(
        self, *, str_: str | None = None, plugins: list[Plugin] | None = None
    ) -> HttpRequestWithRegexLiteralOutput:
        """
        Invokes the HttpRequestWithRegexLiteral operation.

        Args:
            str_:
                The `str_` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `HttpRequestWithRegexLiteralOutput`.
        """
        input = HttpRequestWithRegexLiteralInput(str_=str_)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=HTTP_REQUEST_WITH_REGEX_LITERAL,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def http_response_code(
        self, *, plugins: list[Plugin] | None = None
    ) -> HttpResponseCodeOutput:
        """
        Invokes the HttpResponseCode operation.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `HttpResponseCodeOutput`.
        """
        input = HttpResponseCodeInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=HTTP_RESPONSE_CODE,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def http_string_payload(
        self, *, payload: str | None = None, plugins: list[Plugin] | None = None
    ) -> HttpStringPayloadOutput:
        """
        Invokes the HttpStringPayload operation.

        Args:
            payload:
                The `payload` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `HttpStringPayloadOutput`.
        """
        input = HttpStringPayloadInput(payload=payload)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=HTTP_STRING_PAYLOAD,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def ignore_query_params_in_response(
        self, *, plugins: list[Plugin] | None = None
    ) -> IgnoreQueryParamsInResponseOutput:
        """
        This example ensures that query string bound request parameters are
        serialized in the body of responses if the structure is used in both the
        request and response.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `IgnoreQueryParamsInResponseOutput`.
        """
        input = IgnoreQueryParamsInResponseInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=IGNORE_QUERY_PARAMS_IN_RESPONSE,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def input_and_output_with_headers(
        self,
        *,
        header_string: str | None = None,
        header_byte: int | None = None,
        header_short: int | None = None,
        header_integer: int | None = None,
        header_long: int | None = None,
        header_float: float | None = None,
        header_double: float | None = None,
        header_true_bool: bool | None = None,
        header_false_bool: bool | None = None,
        header_string_list: list[str] | None = None,
        header_string_set: list[str] | None = None,
        header_integer_list: list[int] | None = None,
        header_boolean_list: list[bool] | None = None,
        header_timestamp_list: list[datetime] | None = None,
        header_enum: str | None = None,
        header_enum_list: list[str] | None = None,
        header_integer_enum: int | None = None,
        header_integer_enum_list: list[int] | None = None,
        plugins: list[Plugin] | None = None,
    ) -> InputAndOutputWithHeadersOutput:
        """
        The example tests how requests and responses are serialized when there
        is no input or output payload but there are HTTP header bindings.

        Args:
            header_string:
                The `header_string` input member.
            header_byte:
                The `header_byte` input member.
            header_short:
                The `header_short` input member.
            header_integer:
                The `header_integer` input member.
            header_long:
                The `header_long` input member.
            header_float:
                The `header_float` input member.
            header_double:
                The `header_double` input member.
            header_true_bool:
                The `header_true_bool` input member.
            header_false_bool:
                The `header_false_bool` input member.
            header_string_list:
                The `header_string_list` input member.
            header_string_set:
                The `header_string_set` input member.
            header_integer_list:
                The `header_integer_list` input member.
            header_boolean_list:
                The `header_boolean_list` input member.
            header_timestamp_list:
                The `header_timestamp_list` input member.
            header_enum:
                The `header_enum` input member.
            header_enum_list:
                The `header_enum_list` input member.
            header_integer_enum:
                The `header_integer_enum` input member.
            header_integer_enum_list:
                The `header_integer_enum_list` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `InputAndOutputWithHeadersOutput`.
        """
        input = InputAndOutputWithHeadersInput(
            header_string=header_string,
            header_byte=header_byte,
            header_short=header_short,
            header_integer=header_integer,
            header_long=header_long,
            header_float=header_float,
            header_double=header_double,
            header_true_bool=header_true_bool,
            header_false_bool=header_false_bool,
            header_string_list=header_string_list,
            header_string_set=header_string_set,
            header_integer_list=header_integer_list,
            header_boolean_list=header_boolean_list,
            header_timestamp_list=header_timestamp_list,
            header_enum=header_enum,
            header_enum_list=header_enum_list,
            header_integer_enum=header_integer_enum,
            header_integer_enum_list=header_integer_enum_list,
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=INPUT_AND_OUTPUT_WITH_HEADERS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def input_stream(
        self, *, plugins: list[Plugin] | None = None
    ) -> InputEventStream[EventStream, InputStreamOutput]:
        """
        Invokes the InputStream operation.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An `InputEventStream` for client-to-server streaming.
        """
        input = InputStreamInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=INPUT_STREAM,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline.input_stream(call, EventStream)

    async def input_stream_with_initial_request(
        self,
        *,
        initial_request_member: str | None = None,
        plugins: list[Plugin] | None = None,
    ) -> InputEventStream[EventStream, InputStreamWithInitialRequestOutput]:
        """
        Invokes the InputStreamWithInitialRequest operation.

        Args:
            initial_request_member:
                The `initial_request_member` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An `InputEventStream` for client-to-server streaming.
        """
        input = InputStreamWithInitialRequestInput(
            initial_request_member=initial_request_member
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=INPUT_STREAM_WITH_INITIAL_REQUEST,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline.input_stream(call, EventStream)

    async def json_blobs(
        self, *, data: bytes | None = None, plugins: list[Plugin] | None = None
    ) -> JsonBlobsOutput:
        """
        Blobs are base64 encoded

        Args:
            data:
                The `data` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `JsonBlobsOutput`.
        """
        input = JsonBlobsInput(data=data)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=JSON_BLOBS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def json_enums(
        self,
        *,
        foo_enum1: str | None = None,
        foo_enum2: str | None = None,
        foo_enum3: str | None = None,
        foo_enum_list: list[str] | None = None,
        foo_enum_set: list[str] | None = None,
        foo_enum_map: dict[str, str] | None = None,
        plugins: list[Plugin] | None = None,
    ) -> JsonEnumsOutput:
        """
        This example serializes enums as top level properties, in lists, sets,
        and maps.

        Args:
            foo_enum1:
                The `foo_enum1` input member.
            foo_enum2:
                The `foo_enum2` input member.
            foo_enum3:
                The `foo_enum3` input member.
            foo_enum_list:
                The `foo_enum_list` input member.
            foo_enum_set:
                The `foo_enum_set` input member.
            foo_enum_map:
                The `foo_enum_map` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `JsonEnumsOutput`.
        """
        input = JsonEnumsInput(
            foo_enum1=foo_enum1,
            foo_enum2=foo_enum2,
            foo_enum3=foo_enum3,
            foo_enum_list=foo_enum_list,
            foo_enum_set=foo_enum_set,
            foo_enum_map=foo_enum_map,
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=JSON_ENUMS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def json_int_enums(
        self,
        *,
        integer_enum1: int | None = None,
        integer_enum2: int | None = None,
        integer_enum3: int | None = None,
        integer_enum_list: list[int] | None = None,
        integer_enum_set: list[int] | None = None,
        integer_enum_map: dict[str, int] | None = None,
        plugins: list[Plugin] | None = None,
    ) -> JsonIntEnumsOutput:
        """
        This example serializes intEnums as top level properties, in lists,
        sets, and maps.

        Args:
            integer_enum1:
                The `integer_enum1` input member.
            integer_enum2:
                The `integer_enum2` input member.
            integer_enum3:
                The `integer_enum3` input member.
            integer_enum_list:
                The `integer_enum_list` input member.
            integer_enum_set:
                The `integer_enum_set` input member.
            integer_enum_map:
                The `integer_enum_map` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `JsonIntEnumsOutput`.
        """
        input = JsonIntEnumsInput(
            integer_enum1=integer_enum1,
            integer_enum2=integer_enum2,
            integer_enum3=integer_enum3,
            integer_enum_list=integer_enum_list,
            integer_enum_set=integer_enum_set,
            integer_enum_map=integer_enum_map,
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=JSON_INT_ENUMS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def json_lists(
        self,
        *,
        string_list: list[str] | None = None,
        string_set: list[str] | None = None,
        integer_list: list[int] | None = None,
        boolean_list: list[bool] | None = None,
        timestamp_list: list[datetime] | None = None,
        enum_list: list[str] | None = None,
        int_enum_list: list[int] | None = None,
        nested_string_list: list[list[str]] | None = None,
        structure_list: list[StructureListMember] | None = None,
        plugins: list[Plugin] | None = None,
    ) -> JsonListsOutput:
        """
        This test case serializes JSON lists for the following cases for both
        input and output: 1. Normal JSON lists. 2. Normal JSON sets. 3. JSON
        lists of lists. 4. Lists of structures.

        Args:
            string_list:
                The `string_list` input member.
            string_set:
                The `string_set` input member.
            integer_list:
                The `integer_list` input member.
            boolean_list:
                The `boolean_list` input member.
            timestamp_list:
                The `timestamp_list` input member.
            enum_list:
                The `enum_list` input member.
            int_enum_list:
                The `int_enum_list` input member.
            nested_string_list:
                A list of lists of strings.
            structure_list:
                The `structure_list` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `JsonListsOutput`.
        """
        input = JsonListsInput(
            string_list=string_list,
            string_set=string_set,
            integer_list=integer_list,
            boolean_list=boolean_list,
            timestamp_list=timestamp_list,
            enum_list=enum_list,
            int_enum_list=int_enum_list,
            nested_string_list=nested_string_list,
            structure_list=structure_list,
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=JSON_LISTS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def json_maps(
        self,
        *,
        dense_struct_map: dict[str, GreetingStruct] | None = None,
        dense_number_map: dict[str, int] | None = None,
        dense_boolean_map: dict[str, bool] | None = None,
        dense_string_map: dict[str, str] | None = None,
        dense_set_map: dict[str, list[str]] | None = None,
        plugins: list[Plugin] | None = None,
    ) -> JsonMapsOutput:
        """
        The example tests basic map serialization.

        Args:
            dense_struct_map:
                The `dense_struct_map` input member.
            dense_number_map:
                The `dense_number_map` input member.
            dense_boolean_map:
                The `dense_boolean_map` input member.
            dense_string_map:
                The `dense_string_map` input member.
            dense_set_map:
                The `dense_set_map` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `JsonMapsOutput`.
        """
        input = JsonMapsInput(
            dense_struct_map=dense_struct_map,
            dense_number_map=dense_number_map,
            dense_boolean_map=dense_boolean_map,
            dense_string_map=dense_string_map,
            dense_set_map=dense_set_map,
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=JSON_MAPS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def json_timestamps(
        self,
        *,
        normal: datetime | None = None,
        date_time: datetime | None = None,
        date_time_on_target: datetime | None = None,
        epoch_seconds: datetime | None = None,
        epoch_seconds_on_target: datetime | None = None,
        http_date: datetime | None = None,
        http_date_on_target: datetime | None = None,
        plugins: list[Plugin] | None = None,
    ) -> JsonTimestampsOutput:
        """
        This tests how timestamps are serialized, including using the default
        format of date-time and various @timestampFormat trait values.

        Args:
            normal:
                The `normal` input member.
            date_time:
                The `date_time` input member.
            date_time_on_target:
                The `date_time_on_target` input member.
            epoch_seconds:
                The `epoch_seconds` input member.
            epoch_seconds_on_target:
                The `epoch_seconds_on_target` input member.
            http_date:
                The `http_date` input member.
            http_date_on_target:
                The `http_date_on_target` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `JsonTimestampsOutput`.
        """
        input = JsonTimestampsInput(
            normal=normal,
            date_time=date_time,
            date_time_on_target=date_time_on_target,
            epoch_seconds=epoch_seconds,
            epoch_seconds_on_target=epoch_seconds_on_target,
            http_date=http_date,
            http_date_on_target=http_date_on_target,
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=JSON_TIMESTAMPS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def json_unions(
        self, *, contents: MyUnion | None = None, plugins: list[Plugin] | None = None
    ) -> JsonUnionsOutput:
        """
        This operation uses unions for inputs and outputs.

        Args:
            contents:
                A union with a representative set of types for members.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `JsonUnionsOutput`.
        """
        input = JsonUnionsInput(contents=contents)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=JSON_UNIONS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_accept_with_body(
        self, *, plugins: list[Plugin] | None = None
    ) -> MalformedAcceptWithBodyOutput:
        """
        Invokes the MalformedAcceptWithBody operation.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedAcceptWithBodyOutput`.
        """
        input = MalformedAcceptWithBodyInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_ACCEPT_WITH_BODY,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_accept_with_generic_string(
        self, *, plugins: list[Plugin] | None = None
    ) -> MalformedAcceptWithGenericStringOutput:
        """
        Invokes the MalformedAcceptWithGenericString operation.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedAcceptWithGenericStringOutput`.
        """
        input = MalformedAcceptWithGenericStringInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_ACCEPT_WITH_GENERIC_STRING,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_accept_with_payload(
        self, *, plugins: list[Plugin] | None = None
    ) -> MalformedAcceptWithPayloadOutput:
        """
        Invokes the MalformedAcceptWithPayload operation.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedAcceptWithPayloadOutput`.
        """
        input = MalformedAcceptWithPayloadInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_ACCEPT_WITH_PAYLOAD,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_blob(
        self, *, blob: bytes | None = None, plugins: list[Plugin] | None = None
    ) -> MalformedBlobOutput:
        """
        Invokes the MalformedBlob operation.

        Args:
            blob:
                The `blob` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedBlobOutput`.
        """
        input = MalformedBlobInput(blob=blob)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_BLOB,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_boolean(
        self,
        *,
        boolean_in_body: bool | None = None,
        boolean_in_path: bool | None = None,
        boolean_in_query: bool | None = None,
        boolean_in_header: bool | None = None,
        plugins: list[Plugin] | None = None,
    ) -> MalformedBooleanOutput:
        """
        Invokes the MalformedBoolean operation.

        Args:
            boolean_in_body:
                The `boolean_in_body` input member.
            boolean_in_path:
                The `boolean_in_path` input member.
            boolean_in_query:
                The `boolean_in_query` input member.
            boolean_in_header:
                The `boolean_in_header` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedBooleanOutput`.
        """
        input = MalformedBooleanInput(
            boolean_in_body=boolean_in_body,
            boolean_in_path=boolean_in_path,
            boolean_in_query=boolean_in_query,
            boolean_in_header=boolean_in_header,
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_BOOLEAN,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_byte(
        self,
        *,
        byte_in_body: int | None = None,
        byte_in_path: int | None = None,
        byte_in_query: int | None = None,
        byte_in_header: int | None = None,
        plugins: list[Plugin] | None = None,
    ) -> MalformedByteOutput:
        """
        Invokes the MalformedByte operation.

        Args:
            byte_in_body:
                The `byte_in_body` input member.
            byte_in_path:
                The `byte_in_path` input member.
            byte_in_query:
                The `byte_in_query` input member.
            byte_in_header:
                The `byte_in_header` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedByteOutput`.
        """
        input = MalformedByteInput(
            byte_in_body=byte_in_body,
            byte_in_path=byte_in_path,
            byte_in_query=byte_in_query,
            byte_in_header=byte_in_header,
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_BYTE,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_content_type_with_body(
        self, *, hi: str | None = None, plugins: list[Plugin] | None = None
    ) -> MalformedContentTypeWithBodyOutput:
        """
        Invokes the MalformedContentTypeWithBody operation.

        Args:
            hi:
                The `hi` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedContentTypeWithBodyOutput`.
        """
        input = MalformedContentTypeWithBodyInput(hi=hi)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_CONTENT_TYPE_WITH_BODY,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_content_type_with_generic_string(
        self, *, payload: str | None = None, plugins: list[Plugin] | None = None
    ) -> MalformedContentTypeWithGenericStringOutput:
        """
        Invokes the MalformedContentTypeWithGenericString operation.

        Args:
            payload:
                The `payload` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedContentTypeWithGenericStringOutput`.
        """
        input = MalformedContentTypeWithGenericStringInput(payload=payload)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_CONTENT_TYPE_WITH_GENERIC_STRING,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_content_type_without_body(
        self, *, plugins: list[Plugin] | None = None
    ) -> MalformedContentTypeWithoutBodyOutput:
        """
        Invokes the MalformedContentTypeWithoutBody operation.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedContentTypeWithoutBodyOutput`.
        """
        input = MalformedContentTypeWithoutBodyInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_CONTENT_TYPE_WITHOUT_BODY,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_content_type_without_body_empty_input(
        self, *, header: str | None = None, plugins: list[Plugin] | None = None
    ) -> MalformedContentTypeWithoutBodyEmptyInputOutput:
        """
        Invokes the MalformedContentTypeWithoutBodyEmptyInput operation.

        Args:
            header:
                The `header` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedContentTypeWithoutBodyEmptyInputOutput`.
        """
        input = MalformedContentTypeWithoutBodyEmptyInputInput(header=header)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_CONTENT_TYPE_WITHOUT_BODY_EMPTY_INPUT,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_content_type_with_payload(
        self, *, payload: bytes | None = None, plugins: list[Plugin] | None = None
    ) -> MalformedContentTypeWithPayloadOutput:
        """
        Invokes the MalformedContentTypeWithPayload operation.

        Args:
            payload:
                The `payload` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedContentTypeWithPayloadOutput`.
        """
        input = MalformedContentTypeWithPayloadInput(payload=payload)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_CONTENT_TYPE_WITH_PAYLOAD,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_double(
        self,
        *,
        double_in_body: float | None = None,
        double_in_path: float | None = None,
        double_in_query: float | None = None,
        double_in_header: float | None = None,
        plugins: list[Plugin] | None = None,
    ) -> MalformedDoubleOutput:
        """
        Invokes the MalformedDouble operation.

        Args:
            double_in_body:
                The `double_in_body` input member.
            double_in_path:
                The `double_in_path` input member.
            double_in_query:
                The `double_in_query` input member.
            double_in_header:
                The `double_in_header` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedDoubleOutput`.
        """
        input = MalformedDoubleInput(
            double_in_body=double_in_body,
            double_in_path=double_in_path,
            double_in_query=double_in_query,
            double_in_header=double_in_header,
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_DOUBLE,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_float(
        self,
        *,
        float_in_body: float | None = None,
        float_in_path: float | None = None,
        float_in_query: float | None = None,
        float_in_header: float | None = None,
        plugins: list[Plugin] | None = None,
    ) -> MalformedFloatOutput:
        """
        Invokes the MalformedFloat operation.

        Args:
            float_in_body:
                The `float_in_body` input member.
            float_in_path:
                The `float_in_path` input member.
            float_in_query:
                The `float_in_query` input member.
            float_in_header:
                The `float_in_header` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedFloatOutput`.
        """
        input = MalformedFloatInput(
            float_in_body=float_in_body,
            float_in_path=float_in_path,
            float_in_query=float_in_query,
            float_in_header=float_in_header,
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_FLOAT,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_integer(
        self,
        *,
        integer_in_body: int | None = None,
        integer_in_path: int | None = None,
        integer_in_query: int | None = None,
        integer_in_header: int | None = None,
        plugins: list[Plugin] | None = None,
    ) -> MalformedIntegerOutput:
        """
        Invokes the MalformedInteger operation.

        Args:
            integer_in_body:
                The `integer_in_body` input member.
            integer_in_path:
                The `integer_in_path` input member.
            integer_in_query:
                The `integer_in_query` input member.
            integer_in_header:
                The `integer_in_header` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedIntegerOutput`.
        """
        input = MalformedIntegerInput(
            integer_in_body=integer_in_body,
            integer_in_path=integer_in_path,
            integer_in_query=integer_in_query,
            integer_in_header=integer_in_header,
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_INTEGER,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_list(
        self, *, body_list: list[str] | None = None, plugins: list[Plugin] | None = None
    ) -> MalformedListOutput:
        """
        Invokes the MalformedList operation.

        Args:
            body_list:
                The `body_list` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedListOutput`.
        """
        input = MalformedListInput(body_list=body_list)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_LIST,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_long(
        self,
        *,
        long_in_body: int | None = None,
        long_in_path: int | None = None,
        long_in_query: int | None = None,
        long_in_header: int | None = None,
        plugins: list[Plugin] | None = None,
    ) -> MalformedLongOutput:
        """
        Invokes the MalformedLong operation.

        Args:
            long_in_body:
                The `long_in_body` input member.
            long_in_path:
                The `long_in_path` input member.
            long_in_query:
                The `long_in_query` input member.
            long_in_header:
                The `long_in_header` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedLongOutput`.
        """
        input = MalformedLongInput(
            long_in_body=long_in_body,
            long_in_path=long_in_path,
            long_in_query=long_in_query,
            long_in_header=long_in_header,
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_LONG,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_map(
        self,
        *,
        body_map: dict[str, str] | None = None,
        plugins: list[Plugin] | None = None,
    ) -> MalformedMapOutput:
        """
        Invokes the MalformedMap operation.

        Args:
            body_map:
                The `body_map` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedMapOutput`.
        """
        input = MalformedMapInput(body_map=body_map)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_MAP,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_request_body(
        self,
        *,
        int_: int | None = None,
        float_: float | None = None,
        plugins: list[Plugin] | None = None,
    ) -> MalformedRequestBodyOutput:
        """
        Invokes the MalformedRequestBody operation.

        Args:
            int_:
                The `int_` input member.
            float_:
                The `float_` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedRequestBodyOutput`.
        """
        input = MalformedRequestBodyInput(int_=int_, float_=float_)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_REQUEST_BODY,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_short(
        self,
        *,
        short_in_body: int | None = None,
        short_in_path: int | None = None,
        short_in_query: int | None = None,
        short_in_header: int | None = None,
        plugins: list[Plugin] | None = None,
    ) -> MalformedShortOutput:
        """
        Invokes the MalformedShort operation.

        Args:
            short_in_body:
                The `short_in_body` input member.
            short_in_path:
                The `short_in_path` input member.
            short_in_query:
                The `short_in_query` input member.
            short_in_header:
                The `short_in_header` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedShortOutput`.
        """
        input = MalformedShortInput(
            short_in_body=short_in_body,
            short_in_path=short_in_path,
            short_in_query=short_in_query,
            short_in_header=short_in_header,
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_SHORT,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_string(
        self,
        *,
        blob: str | JsonString | None = None,
        plugins: list[Plugin] | None = None,
    ) -> MalformedStringOutput:
        """
        Invokes the MalformedString operation.

        Args:
            blob:
                The `blob` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedStringOutput`.
        """
        input = MalformedStringInput(blob=blob)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_STRING,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_timestamp_body_date_time(
        self, *, timestamp: datetime | None = None, plugins: list[Plugin] | None = None
    ) -> MalformedTimestampBodyDateTimeOutput:
        """
        Invokes the MalformedTimestampBodyDateTime operation.

        Args:
            timestamp:
                The `timestamp` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedTimestampBodyDateTimeOutput`.
        """
        input = MalformedTimestampBodyDateTimeInput(timestamp=timestamp)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_TIMESTAMP_BODY_DATE_TIME,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_timestamp_body_default(
        self, *, timestamp: datetime | None = None, plugins: list[Plugin] | None = None
    ) -> MalformedTimestampBodyDefaultOutput:
        """
        Invokes the MalformedTimestampBodyDefault operation.

        Args:
            timestamp:
                The `timestamp` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedTimestampBodyDefaultOutput`.
        """
        input = MalformedTimestampBodyDefaultInput(timestamp=timestamp)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_TIMESTAMP_BODY_DEFAULT,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_timestamp_body_http_date(
        self, *, timestamp: datetime | None = None, plugins: list[Plugin] | None = None
    ) -> MalformedTimestampBodyHttpDateOutput:
        """
        Invokes the MalformedTimestampBodyHttpDate operation.

        Args:
            timestamp:
                The `timestamp` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedTimestampBodyHttpDateOutput`.
        """
        input = MalformedTimestampBodyHttpDateInput(timestamp=timestamp)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_TIMESTAMP_BODY_HTTP_DATE,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_timestamp_header_date_time(
        self, *, timestamp: datetime | None = None, plugins: list[Plugin] | None = None
    ) -> MalformedTimestampHeaderDateTimeOutput:
        """
        Invokes the MalformedTimestampHeaderDateTime operation.

        Args:
            timestamp:
                The `timestamp` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedTimestampHeaderDateTimeOutput`.
        """
        input = MalformedTimestampHeaderDateTimeInput(timestamp=timestamp)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_TIMESTAMP_HEADER_DATE_TIME,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_timestamp_header_default(
        self, *, timestamp: datetime | None = None, plugins: list[Plugin] | None = None
    ) -> MalformedTimestampHeaderDefaultOutput:
        """
        Invokes the MalformedTimestampHeaderDefault operation.

        Args:
            timestamp:
                The `timestamp` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedTimestampHeaderDefaultOutput`.
        """
        input = MalformedTimestampHeaderDefaultInput(timestamp=timestamp)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_TIMESTAMP_HEADER_DEFAULT,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_timestamp_header_epoch(
        self, *, timestamp: datetime | None = None, plugins: list[Plugin] | None = None
    ) -> MalformedTimestampHeaderEpochOutput:
        """
        Invokes the MalformedTimestampHeaderEpoch operation.

        Args:
            timestamp:
                The `timestamp` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedTimestampHeaderEpochOutput`.
        """
        input = MalformedTimestampHeaderEpochInput(timestamp=timestamp)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_TIMESTAMP_HEADER_EPOCH,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_timestamp_path_default(
        self, *, timestamp: datetime | None = None, plugins: list[Plugin] | None = None
    ) -> MalformedTimestampPathDefaultOutput:
        """
        Invokes the MalformedTimestampPathDefault operation.

        Args:
            timestamp:
                The `timestamp` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedTimestampPathDefaultOutput`.
        """
        input = MalformedTimestampPathDefaultInput(timestamp=timestamp)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_TIMESTAMP_PATH_DEFAULT,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_timestamp_path_epoch(
        self, *, timestamp: datetime | None = None, plugins: list[Plugin] | None = None
    ) -> MalformedTimestampPathEpochOutput:
        """
        Invokes the MalformedTimestampPathEpoch operation.

        Args:
            timestamp:
                The `timestamp` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedTimestampPathEpochOutput`.
        """
        input = MalformedTimestampPathEpochInput(timestamp=timestamp)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_TIMESTAMP_PATH_EPOCH,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_timestamp_path_http_date(
        self, *, timestamp: datetime | None = None, plugins: list[Plugin] | None = None
    ) -> MalformedTimestampPathHttpDateOutput:
        """
        Invokes the MalformedTimestampPathHttpDate operation.

        Args:
            timestamp:
                The `timestamp` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedTimestampPathHttpDateOutput`.
        """
        input = MalformedTimestampPathHttpDateInput(timestamp=timestamp)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_TIMESTAMP_PATH_HTTP_DATE,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_timestamp_query_default(
        self, *, timestamp: datetime | None = None, plugins: list[Plugin] | None = None
    ) -> MalformedTimestampQueryDefaultOutput:
        """
        Invokes the MalformedTimestampQueryDefault operation.

        Args:
            timestamp:
                The `timestamp` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedTimestampQueryDefaultOutput`.
        """
        input = MalformedTimestampQueryDefaultInput(timestamp=timestamp)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_TIMESTAMP_QUERY_DEFAULT,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_timestamp_query_epoch(
        self, *, timestamp: datetime | None = None, plugins: list[Plugin] | None = None
    ) -> MalformedTimestampQueryEpochOutput:
        """
        Invokes the MalformedTimestampQueryEpoch operation.

        Args:
            timestamp:
                The `timestamp` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedTimestampQueryEpochOutput`.
        """
        input = MalformedTimestampQueryEpochInput(timestamp=timestamp)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_TIMESTAMP_QUERY_EPOCH,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_timestamp_query_http_date(
        self, *, timestamp: datetime | None = None, plugins: list[Plugin] | None = None
    ) -> MalformedTimestampQueryHttpDateOutput:
        """
        Invokes the MalformedTimestampQueryHttpDate operation.

        Args:
            timestamp:
                The `timestamp` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedTimestampQueryHttpDateOutput`.
        """
        input = MalformedTimestampQueryHttpDateInput(timestamp=timestamp)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_TIMESTAMP_QUERY_HTTP_DATE,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def malformed_union(
        self, *, union: SimpleUnion | None = None, plugins: list[Plugin] | None = None
    ) -> MalformedUnionOutput:
        """
        Invokes the MalformedUnion operation.

        Args:
            union:
                The `union` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MalformedUnionOutput`.
        """
        input = MalformedUnionInput(union=union)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MALFORMED_UNION,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def media_type_header(
        self,
        *,
        json: str | JsonString | None = None,
        plugins: list[Plugin] | None = None,
    ) -> MediaTypeHeaderOutput:
        """
        This example ensures that mediaType strings are base64 encoded in
        headers.

        Args:
            json:
                The `json` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `MediaTypeHeaderOutput`.
        """
        input = MediaTypeHeaderInput(json=json)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=MEDIA_TYPE_HEADER,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def no_input_and_no_output(
        self, *, plugins: list[Plugin] | None = None
    ) -> NoInputAndNoOutputOutput:
        """
        The example tests how requests and responses are serialized when
        there's no request or response payload because the operation has no
        input or output. While this should be rare, code generators must support
        this.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `NoInputAndNoOutputOutput`.
        """
        input = NoInputAndNoOutputInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=NO_INPUT_AND_NO_OUTPUT,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def no_input_and_output(
        self, *, plugins: list[Plugin] | None = None
    ) -> NoInputAndOutputOutput:
        """
        The example tests how requests and responses are serialized when
        there's no request or response payload because the operation has no
        input and the output is empty. While this should be rare, code
        generators must support this.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `NoInputAndOutputOutput`.
        """
        input = NoInputAndOutputInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=NO_INPUT_AND_OUTPUT,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def null_and_empty_headers_client(
        self,
        *,
        a: str | None = None,
        b: str | None = None,
        c: list[str] | None = None,
        plugins: list[Plugin] | None = None,
    ) -> NullAndEmptyHeadersClientOutput:
        """
        Null headers are not sent over the wire, empty headers are serialized to
        \"\"

        Args:
            a:
                The `a` input member.
            b:
                The `b` input member.
            c:
                The `c` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `NullAndEmptyHeadersClientOutput`.
        """
        input = NullAndEmptyHeadersClientInput(a=a, b=b, c=c)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=NULL_AND_EMPTY_HEADERS_CLIENT,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def null_and_empty_headers_server(
        self,
        *,
        a: str | None = None,
        b: str | None = None,
        c: list[str] | None = None,
        plugins: list[Plugin] | None = None,
    ) -> NullAndEmptyHeadersServerOutput:
        """
        Null headers are not sent over the wire, empty headers are serialized to
        \"\"

        Args:
            a:
                The `a` input member.
            b:
                The `b` input member.
            c:
                The `c` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `NullAndEmptyHeadersServerOutput`.
        """
        input = NullAndEmptyHeadersServerInput(a=a, b=b, c=c)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=NULL_AND_EMPTY_HEADERS_SERVER,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def omits_null_serializes_empty_string(
        self,
        *,
        null_value: str | None = None,
        empty_string: str | None = None,
        plugins: list[Plugin] | None = None,
    ) -> OmitsNullSerializesEmptyStringOutput:
        """
        Omits null, but serializes empty string value.

        Args:
            null_value:
                The `null_value` input member.
            empty_string:
                The `empty_string` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `OmitsNullSerializesEmptyStringOutput`.
        """
        input = OmitsNullSerializesEmptyStringInput(
            null_value=null_value, empty_string=empty_string
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=OMITS_NULL_SERIALIZES_EMPTY_STRING,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def omits_serializing_empty_lists(
        self,
        *,
        query_string_list: list[str] | None = None,
        query_integer_list: list[int] | None = None,
        query_double_list: list[float] | None = None,
        query_boolean_list: list[bool] | None = None,
        query_timestamp_list: list[datetime] | None = None,
        query_enum_list: list[str] | None = None,
        query_integer_enum_list: list[int] | None = None,
        plugins: list[Plugin] | None = None,
    ) -> OmitsSerializingEmptyListsOutput:
        """
        Omits serializing empty lists. Because empty strings are serilized as
        `Foo=`, empty lists cannot also be serialized as `Foo=` and instead
        must be omitted.

        Args:
            query_string_list:
                The `query_string_list` input member.
            query_integer_list:
                The `query_integer_list` input member.
            query_double_list:
                The `query_double_list` input member.
            query_boolean_list:
                The `query_boolean_list` input member.
            query_timestamp_list:
                The `query_timestamp_list` input member.
            query_enum_list:
                The `query_enum_list` input member.
            query_integer_enum_list:
                The `query_integer_enum_list` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `OmitsSerializingEmptyListsOutput`.
        """
        input = OmitsSerializingEmptyListsInput(
            query_string_list=query_string_list,
            query_integer_list=query_integer_list,
            query_double_list=query_double_list,
            query_boolean_list=query_boolean_list,
            query_timestamp_list=query_timestamp_list,
            query_enum_list=query_enum_list,
            query_integer_enum_list=query_integer_enum_list,
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=OMITS_SERIALIZING_EMPTY_LISTS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def operation_with_defaults(
        self,
        *,
        defaults: Defaults | None = None,
        client_optional_defaults: ClientOptionalDefaults | None = None,
        top_level_default: str = "hi",
        other_top_level_default: int = 0,
        plugins: list[Plugin] | None = None,
    ) -> OperationWithDefaultsOutput:
        """
        Invokes the OperationWithDefaults operation.

        Args:
            defaults:
                The `defaults` input member.
            client_optional_defaults:
                The `client_optional_defaults` input member.
            top_level_default:
                The `top_level_default` input member.
            other_top_level_default:
                The `other_top_level_default` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `OperationWithDefaultsOutput`.
        """
        input = OperationWithDefaultsInput(
            defaults=defaults,
            client_optional_defaults=client_optional_defaults,
            top_level_default=top_level_default,
            other_top_level_default=other_top_level_default,
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=OPERATION_WITH_DEFAULTS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def operation_with_nested_structure(
        self, *, top_level: TopLevel | None = None, plugins: list[Plugin] | None = None
    ) -> OperationWithNestedStructureOutput:
        """
        Invokes the OperationWithNestedStructure operation.

        Args:
            top_level:
                The `top_level` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `OperationWithNestedStructureOutput`.
        """
        input = OperationWithNestedStructureInput(top_level=top_level)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=OPERATION_WITH_NESTED_STRUCTURE,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def output_stream(
        self, *, plugins: list[Plugin] | None = None
    ) -> OutputEventStream[EventStream, OutputStreamOutput]:
        """
        Invokes the OutputStream operation.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An `OutputEventStream` for server-to-client streaming.
        """
        input = OutputStreamInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=OUTPUT_STREAM,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline.output_stream(
            call, EventStream, _EventStreamDeserializer().deserialize
        )

    async def output_stream_with_initial_response(
        self, *, plugins: list[Plugin] | None = None
    ) -> OutputEventStream[EventStream, OutputStreamWithInitialResponseOutput]:
        """
        Invokes the OutputStreamWithInitialResponse operation.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An `OutputEventStream` for server-to-client streaming.
        """
        input = OutputStreamWithInitialResponseInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=OUTPUT_STREAM_WITH_INITIAL_RESPONSE,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline.output_stream(
            call, EventStream, _EventStreamDeserializer().deserialize
        )

    async def post_player_action(
        self, *, action: PlayerAction | None = None, plugins: list[Plugin] | None = None
    ) -> PostPlayerActionOutput:
        """
        This operation defines a union with a Unit member.

        Args:
            action:
                The `action` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `PostPlayerActionOutput`.
        """
        input = PostPlayerActionInput(action=action)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=POST_PLAYER_ACTION,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def post_union_with_json_name(
        self,
        *,
        value: UnionWithJsonName | None = None,
        plugins: list[Plugin] | None = None,
    ) -> PostUnionWithJsonNameOutput:
        """
        This operation defines a union that uses jsonName on some members.

        Args:
            value:
                The `value` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `PostUnionWithJsonNameOutput`.
        """
        input = PostUnionWithJsonNameInput(value=value)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=POST_UNION_WITH_JSON_NAME,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def put_with_content_encoding(
        self,
        *,
        encoding: str | None = None,
        data: str | None = None,
        plugins: list[Plugin] | None = None,
    ) -> PutWithContentEncodingOutput:
        """
        Invokes the PutWithContentEncoding operation.

        Args:
            encoding:
                The `encoding` input member.
            data:
                The `data` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `PutWithContentEncodingOutput`.
        """
        input = PutWithContentEncodingInput(encoding=encoding, data=data)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=PUT_WITH_CONTENT_ENCODING,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def query_idempotency_token_auto_fill(
        self, *, token: str | None = None, plugins: list[Plugin] | None = None
    ) -> QueryIdempotencyTokenAutoFillOutput:
        """
        Automatically adds idempotency tokens.

        Args:
            token:
                The `token` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `QueryIdempotencyTokenAutoFillOutput`.
        """
        input = QueryIdempotencyTokenAutoFillInput(token=token)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=QUERY_IDEMPOTENCY_TOKEN_AUTO_FILL,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def query_params_as_string_list_map(
        self,
        *,
        qux: str | None = None,
        foo: dict[str, list[str]] | None = None,
        plugins: list[Plugin] | None = None,
    ) -> QueryParamsAsStringListMapOutput:
        """
        Invokes the QueryParamsAsStringListMap operation.

        Args:
            qux:
                The `qux` input member.
            foo:
                The `foo` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `QueryParamsAsStringListMapOutput`.
        """
        input = QueryParamsAsStringListMapInput(qux=qux, foo=foo)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=QUERY_PARAMS_AS_STRING_LIST_MAP,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def query_precedence(
        self,
        *,
        foo: str | None = None,
        baz: dict[str, str] | None = None,
        plugins: list[Plugin] | None = None,
    ) -> QueryPrecedenceOutput:
        """
        Invokes the QueryPrecedence operation.

        Args:
            foo:
                The `foo` input member.
            baz:
                The `baz` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `QueryPrecedenceOutput`.
        """
        input = QueryPrecedenceInput(foo=foo, baz=baz)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=QUERY_PRECEDENCE,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def recursive_shapes(
        self,
        *,
        nested: RecursiveShapesInputOutputNested1 | None = None,
        plugins: list[Plugin] | None = None,
    ) -> RecursiveShapesOutput:
        """
        Recursive shapes

        Args:
            nested:
                The `nested` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `RecursiveShapesOutput`.
        """
        input = RecursiveShapesInput(nested=nested)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=RECURSIVE_SHAPES,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def response_code_http_fallback(
        self, *, plugins: list[Plugin] | None = None
    ) -> ResponseCodeHttpFallbackOutput:
        """
        Invokes the ResponseCodeHttpFallback operation.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `ResponseCodeHttpFallbackOutput`.
        """
        input = ResponseCodeHttpFallbackInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=RESPONSE_CODE_HTTP_FALLBACK,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def response_code_required(
        self, *, plugins: list[Plugin] | None = None
    ) -> ResponseCodeRequiredOutput:
        """
        Invokes the ResponseCodeRequired operation.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `ResponseCodeRequiredOutput`.
        """
        input = ResponseCodeRequiredInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=RESPONSE_CODE_REQUIRED,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def simple_scalar_properties(
        self,
        *,
        foo: str | None = None,
        string_value: str | None = None,
        true_boolean_value: bool | None = None,
        false_boolean_value: bool | None = None,
        byte_value: int | None = None,
        short_value: int | None = None,
        integer_value: int | None = None,
        long_value: int | None = None,
        float_value: float | None = None,
        double_value: float | None = None,
        plugins: list[Plugin] | None = None,
    ) -> SimpleScalarPropertiesOutput:
        """
        Invokes the SimpleScalarProperties operation.

        Args:
            foo:
                The `foo` input member.
            string_value:
                The `string_value` input member.
            true_boolean_value:
                The `true_boolean_value` input member.
            false_boolean_value:
                The `false_boolean_value` input member.
            byte_value:
                The `byte_value` input member.
            short_value:
                The `short_value` input member.
            integer_value:
                The `integer_value` input member.
            long_value:
                The `long_value` input member.
            float_value:
                The `float_value` input member.
            double_value:
                The `double_value` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `SimpleScalarPropertiesOutput`.
        """
        input = SimpleScalarPropertiesInput(
            foo=foo,
            string_value=string_value,
            true_boolean_value=true_boolean_value,
            false_boolean_value=false_boolean_value,
            byte_value=byte_value,
            short_value=short_value,
            integer_value=integer_value,
            long_value=long_value,
            float_value=float_value,
            double_value=double_value,
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=SIMPLE_SCALAR_PROPERTIES,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def sparse_json_lists(
        self,
        *,
        sparse_string_list: list[str | None] | None = None,
        sparse_short_list: list[int | None] | None = None,
        plugins: list[Plugin] | None = None,
    ) -> SparseJsonListsOutput:
        """
        Invokes the SparseJsonLists operation.

        Args:
            sparse_string_list:
                The `sparse_string_list` input member.
            sparse_short_list:
                The `sparse_short_list` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `SparseJsonListsOutput`.
        """
        input = SparseJsonListsInput(
            sparse_string_list=sparse_string_list, sparse_short_list=sparse_short_list
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=SPARSE_JSON_LISTS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def sparse_json_maps(
        self,
        *,
        sparse_struct_map: dict[str, GreetingStruct | None] | None = None,
        sparse_number_map: dict[str, int | None] | None = None,
        sparse_boolean_map: dict[str, bool | None] | None = None,
        sparse_string_map: dict[str, str | None] | None = None,
        sparse_set_map: dict[str, list[str] | None] | None = None,
        plugins: list[Plugin] | None = None,
    ) -> SparseJsonMapsOutput:
        """
        This example tests sparse map serialization.

        Args:
            sparse_struct_map:
                The `sparse_struct_map` input member.
            sparse_number_map:
                The `sparse_number_map` input member.
            sparse_boolean_map:
                The `sparse_boolean_map` input member.
            sparse_string_map:
                The `sparse_string_map` input member.
            sparse_set_map:
                The `sparse_set_map` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `SparseJsonMapsOutput`.
        """
        input = SparseJsonMapsInput(
            sparse_struct_map=sparse_struct_map,
            sparse_number_map=sparse_number_map,
            sparse_boolean_map=sparse_boolean_map,
            sparse_string_map=sparse_string_map,
            sparse_set_map=sparse_set_map,
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=SPARSE_JSON_MAPS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def streaming_traits(
        self,
        *,
        foo: str | None = None,
        blob: StreamingBlob = b64decode(""),
        plugins: list[Plugin] | None = None,
    ) -> StreamingTraitsOutput:
        """
        This examples serializes a streaming blob shape in the request body. In
        this example, no JSON document is synthesized because the payload is not
        a structure or a union type.

        Args:
            foo:
                The `foo` input member.
            blob:
                The `blob` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `StreamingTraitsOutput`.
        """
        input = StreamingTraitsInput(foo=foo, blob=blob)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=STREAMING_TRAITS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def streaming_traits_require_length(
        self,
        *,
        foo: str | None = None,
        blob: StreamingBlob = b64decode(""),
        plugins: list[Plugin] | None = None,
    ) -> StreamingTraitsRequireLengthOutput:
        """
        This examples serializes a streaming blob shape with a required content
        length in the request body. In this example, no JSON document is
        synthesized because the payload is not a structure or a union type.

        Args:
            foo:
                The `foo` input member.
            blob:
                The `blob` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `StreamingTraitsRequireLengthOutput`.
        """
        input = StreamingTraitsRequireLengthInput(foo=foo, blob=blob)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=STREAMING_TRAITS_REQUIRE_LENGTH,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def streaming_traits_with_media_type(
        self,
        *,
        foo: str | None = None,
        blob: StreamingBlob = b64decode(""),
        plugins: list[Plugin] | None = None,
    ) -> StreamingTraitsWithMediaTypeOutput:
        """
        This examples serializes a streaming media-typed blob shape in the
        request body. This examples uses a `@mediaType` trait on the payload
        to force a custom content-type to be serialized.

        Args:
            foo:
                The `foo` input member.
            blob:
                The `blob` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `StreamingTraitsWithMediaTypeOutput`.
        """
        input = StreamingTraitsWithMediaTypeInput(foo=foo, blob=blob)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=STREAMING_TRAITS_WITH_MEDIA_TYPE,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def test_body_structure(
        self,
        *,
        test_id: str | None = None,
        test_config: TestConfig | None = None,
        plugins: list[Plugin] | None = None,
    ) -> TestBodyStructureOutput:
        """
        This example operation serializes a structure in the HTTP body. It
        should ensure Content-Type: application/json is used in all requests and
        that an \"empty\" body is an empty JSON document ({}).

        Args:
            test_id:
                The `test_id` input member.
            test_config:
                The `test_config` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `TestBodyStructureOutput`.
        """
        input = TestBodyStructureInput(test_id=test_id, test_config=test_config)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=TEST_BODY_STRUCTURE,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def test_get_no_input_no_payload(
        self, *, plugins: list[Plugin] | None = None
    ) -> TestGetNoInputNoPayloadOutput:
        """
        This example GET operation has no input and serializes a request without
        a HTTP body. These tests are to ensure we do not attach a body or
        related headers (Content-Length, Content-Type) to operations that
        semantically cannot produce an HTTP body.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `TestGetNoInputNoPayloadOutput`.
        """
        input = TestGetNoInputNoPayloadInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=TEST_GET_NO_INPUT_NO_PAYLOAD,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def test_get_no_payload(
        self, *, test_id: str | None = None, plugins: list[Plugin] | None = None
    ) -> TestGetNoPayloadOutput:
        """
        This example GET operation serializes a request without a modeled HTTP
        body. These tests are to ensure we do not attach a body or related
        headers (Content-Length, Content-Type) to operations that semantically
        cannot produce an HTTP body.

        Args:
            test_id:
                The `test_id` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `TestGetNoPayloadOutput`.
        """
        input = TestGetNoPayloadInput(test_id=test_id)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=TEST_GET_NO_PAYLOAD,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def test_payload_blob(
        self,
        *,
        content_type: str | None = None,
        data: bytes | None = None,
        plugins: list[Plugin] | None = None,
    ) -> TestPayloadBlobOutput:
        """
        This example operation serializes a payload targeting a blob. The Blob
        shape is not structured content and we cannot make assumptions about
        what data will be sent. This test ensures only a generic \"Content-Type:
        application/octet-stream\" header is used, and that we are not treating
        an empty body as an empty JSON document.

        Args:
            content_type:
                The `content_type` input member.
            data:
                The `data` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `TestPayloadBlobOutput`.
        """
        input = TestPayloadBlobInput(content_type=content_type, data=data)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=TEST_PAYLOAD_BLOB,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def test_payload_structure(
        self,
        *,
        test_id: str | None = None,
        payload_config: PayloadConfig | None = None,
        plugins: list[Plugin] | None = None,
    ) -> TestPayloadStructureOutput:
        """
        This example operation serializes a payload targeting a structure. This
        enforces the same requirements as TestBodyStructure but with the body
        specified by the @httpPayload trait.

        Args:
            test_id:
                The `test_id` input member.
            payload_config:
                The `payload_config` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `TestPayloadStructureOutput`.
        """
        input = TestPayloadStructureInput(
            test_id=test_id, payload_config=payload_config
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=TEST_PAYLOAD_STRUCTURE,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def test_post_no_input_no_payload(
        self, *, plugins: list[Plugin] | None = None
    ) -> TestPostNoInputNoPayloadOutput:
        """
        This example POST operation has no input and serializes a request
        without a HTTP body. These tests are to ensure we do not attach a body
        or related headers (Content-Type) to a POST operation with no modeled
        input.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `TestPostNoInputNoPayloadOutput`.
        """
        input = TestPostNoInputNoPayloadInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=TEST_POST_NO_INPUT_NO_PAYLOAD,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def test_post_no_payload(
        self, *, test_id: str | None = None, plugins: list[Plugin] | None = None
    ) -> TestPostNoPayloadOutput:
        """
        This example POST operation serializes a request without a modeled HTTP
        body. These tests are to ensure we do not attach a body or related
        headers (Content-Type) to a POST operation with no modeled payload.

        Args:
            test_id:
                The `test_id` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `TestPostNoPayloadOutput`.
        """
        input = TestPostNoPayloadInput(test_id=test_id)
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=TEST_POST_NO_PAYLOAD,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def timestamp_format_headers(
        self,
        *,
        member_epoch_seconds: datetime | None = None,
        member_http_date: datetime | None = None,
        member_date_time: datetime | None = None,
        default_format: datetime | None = None,
        target_epoch_seconds: datetime | None = None,
        target_http_date: datetime | None = None,
        target_date_time: datetime | None = None,
        plugins: list[Plugin] | None = None,
    ) -> TimestampFormatHeadersOutput:
        """
        This example tests how timestamp request and response headers are
        serialized.

        Args:
            member_epoch_seconds:
                The `member_epoch_seconds` input member.
            member_http_date:
                The `member_http_date` input member.
            member_date_time:
                The `member_date_time` input member.
            default_format:
                The `default_format` input member.
            target_epoch_seconds:
                The `target_epoch_seconds` input member.
            target_http_date:
                The `target_http_date` input member.
            target_date_time:
                The `target_date_time` input member.
            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `TimestampFormatHeadersOutput`.
        """
        input = TimestampFormatHeadersInput(
            member_epoch_seconds=member_epoch_seconds,
            member_http_date=member_http_date,
            member_date_time=member_date_time,
            default_format=default_format,
            target_epoch_seconds=target_epoch_seconds,
            target_http_date=target_http_date,
            target_date_time=target_date_time,
        )
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=TIMESTAMP_FORMAT_HEADERS,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)

    async def unit_input_and_output(
        self, *, plugins: list[Plugin] | None = None
    ) -> UnitInputAndOutputOutput:
        """
        This test is similar to NoInputAndNoOutput, but uses explicit Unit
        types.

        Args:

            plugins:
                A list of callables that modify the configuration dynamically.
                Changes made by these plugins only apply for the duration of the
                operation execution and will not affect any other operation
                invocations.

        Returns:
            An instance of `UnitInputAndOutputOutput`.
        """
        input = UnitInputAndOutputInput()
        if self._closed:
            raise RuntimeError(
                "Cannot invoke an operation on a client that has been closed."
            )

        operation_plugins: list[Plugin] = []
        if plugins:
            operation_plugins.extend(plugins)
        await self._ensure_setup()
        assert self._config is not None
        if operation_plugins:
            # Keep operation-plugin mutations scoped to this call.
            config = deepcopy(self._config)
            for plugin in operation_plugins:
                plugin(config)
        else:
            config = self._config
        if (
            config.protocol is None
            or config.transport is None
            or config.endpoint_resolver is None
            or config.auth_scheme_resolver is None
            or config.auth_schemes is None
        ):
            raise ExpectationNotMetError(
                "protocol, transport, endpoint_resolver, auth_scheme_resolver,"
                " and auth_schemes MUST be set on the config to make calls."
            )

        retry_strategy = await self._retry_strategy_resolver.resolve_retry_strategy(
            retry_strategy=config.retry_strategy,
            retry_mode=config.retry_mode,
            max_attempts=config.max_attempts,
        )

        pipeline = RequestPipeline(protocol=config.protocol, transport=config.transport)
        call = ClientCall(
            input=input,
            operation=UNIT_INPUT_AND_OUTPUT,
            context=TypedProperties({"config": config}),
            interceptor=InterceptorChain(config.interceptors),
            auth_scheme_resolver=config.auth_scheme_resolver,
            supported_auth_schemes=config.auth_schemes,
            endpoint_resolver=config.endpoint_resolver,
            retry_strategy=retry_strategy,
        )

        return await pipeline(call)
